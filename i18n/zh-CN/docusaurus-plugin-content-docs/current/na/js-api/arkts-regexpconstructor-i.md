# RegExpConstructor

## constructor

```TypeScript
new(pattern: RegExp | string): RegExp
```

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| pattern | RegExp \| string | 是 |  |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| RegExp |  |

## constructor

```TypeScript
new(pattern: string, flags?: string): RegExp
```

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| pattern | string | 是 |  |
| flags | string | 否 |  |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| RegExp |  |

## constructor

```TypeScript
(pattern: RegExp | string): RegExp
```

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| pattern | RegExp \| string | 是 |  |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| RegExp |  |

## constructor

```TypeScript
(pattern: string, flags?: string): RegExp
```

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| pattern | string | 是 |  |
| flags | string | 否 |  |

**返回值：**

| 类型 | 说明 |
| --- | --- |
| RegExp |  |

## rightContext

```TypeScript
rightContext: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## "$+"

```TypeScript
"$+": string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## "$'"

```TypeScript
"$'": string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $1

```TypeScript
$1: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## prototype

```TypeScript
readonly prototype: RegExp
```

**类型：** RegExp

## "$&"

```TypeScript
"$&": string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $2

```TypeScript
$2: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $3

```TypeScript
$3: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## lastParen

```TypeScript
lastParen: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $4

```TypeScript
$4: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## input

```TypeScript
input: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $5

```TypeScript
$5: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $6

```TypeScript
$6: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $7

```TypeScript
$7: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## "$`"

```TypeScript
"$`": string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $8

```TypeScript
$8: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## lastMatch

```TypeScript
lastMatch: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $9

```TypeScript
$9: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## leftContext

```TypeScript
leftContext: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

## $_

```TypeScript
$_: string
```

**类型：** string

**废弃版本：** legacy feature for browser compatibility

