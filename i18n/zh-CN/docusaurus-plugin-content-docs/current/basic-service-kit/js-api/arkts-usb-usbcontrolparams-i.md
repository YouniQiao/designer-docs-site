# USBControlParams

控制传输参数。

**起始版本：** 8

**废弃版本：** 9

**替代接口：** @ohos.usbManager:usbManager.USBControlParams

**系统能力：** SystemCapability.USB.USBManager

## 导入模块

```TypeScript
import { usb } from '@kit.BasicServicesKit';
```

## request

```TypeScript
request: number
```

请求类型。

**类型：** number

**起始版本：** 8

**废弃版本：** 9

**替代接口：** @ohos.usbManager:usbManager.USBControlParams.request

**系统能力：** SystemCapability.USB.USBManager

## data

```TypeScript
data: Uint8Array
```

用于写入或读取的缓冲区。

**类型：** Uint8Array

**起始版本：** 8

**废弃版本：** 9

**替代接口：** @ohos.usbManager:usbManager.USBControlParams.data

**系统能力：** SystemCapability.USB.USBManager

## reqType

```TypeScript
reqType: USBControlRequestType
```

请求控制类型。

**类型：** USBControlRequestType

**起始版本：** 8

**废弃版本：** 9

**替代接口：** @ohos.usbManager:usbManager.USBControlParams.reqType

**系统能力：** SystemCapability.USB.USBManager

## index

```TypeScript
index: number
```

请求参数value对应的索引值。

**类型：** number

**起始版本：** 8

**废弃版本：** 9

**替代接口：** @ohos.usbManager:usbManager.USBControlParams.index

**系统能力：** SystemCapability.USB.USBManager

## value

```TypeScript
value: number
```

请求参数。

**类型：** number

**起始版本：** 8

**废弃版本：** 9

**替代接口：** @ohos.usbManager:usbManager.USBControlParams.value

**系统能力：** SystemCapability.USB.USBManager

## target

```TypeScript
target: USBRequestTargetType
```

请求目标类型。

**类型：** USBRequestTargetType

**起始版本：** 8

**废弃版本：** 9

**替代接口：** @ohos.usbManager:usbManager.USBControlParams.target

**系统能力：** SystemCapability.USB.USBManager

