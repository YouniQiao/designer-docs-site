# DeviceInfo

设备信息。

**起始版本：** 7

**废弃版本：** 11

**替代接口：** @ohos.distributedDeviceManager:distributedDeviceManager.DeviceBasicInfo

**系统能力：** SystemCapability.DistributedHardware.DeviceManager

**系统接口：** 此接口为系统接口。

## 导入模块

```TypeScript
import { deviceManager } from '@kit.DistributedServiceKit';
```

## deviceType

```TypeScript
deviceType: DeviceType
```

设备类型。

**类型：** DeviceType

**起始版本：** 7

**废弃版本：** 11

**替代接口：** @ohos.distributedDeviceManager:distributedDeviceManager.DeviceBasicInfo.deviceType

**系统能力：** SystemCapability.DistributedHardware.DeviceManager

**系统接口：** 此接口为系统接口。

## authForm

```TypeScript
authForm: AuthForm
```

设备认证类型。

**类型：** AuthForm

**起始版本：** 10

**废弃版本：** 11

**系统能力：** SystemCapability.DistributedHardware.DeviceManager

**系统接口：** 此接口为系统接口。

## range

```TypeScript
range: number
```

发现设备的距离。

**类型：** number

**起始版本：** 9

**废弃版本：** 11

**系统能力：** SystemCapability.DistributedHardware.DeviceManager

**系统接口：** 此接口为系统接口。

## networkId

```TypeScript
networkId: string
```

设备网络标识。

**类型：** string

**起始版本：** 8

**废弃版本：** 11

**替代接口：** @ohos.distributedDeviceManager:distributedDeviceManager.DeviceBasicInfo.networkId

**系统能力：** SystemCapability.DistributedHardware.DeviceManager

**系统接口：** 此接口为系统接口。

## deviceName

```TypeScript
deviceName: string
```

设备名称。

**类型：** string

**起始版本：** 7

**废弃版本：** 11

**替代接口：** @ohos.distributedDeviceManager:distributedDeviceManager.DeviceBasicInfo.deviceName

**系统能力：** SystemCapability.DistributedHardware.DeviceManager

**系统接口：** 此接口为系统接口。

## deviceId

```TypeScript
deviceId: string
```

设备的唯一标识。

**类型：** string

**起始版本：** 7

**废弃版本：** 11

**替代接口：** @ohos.distributedDeviceManager:distributedDeviceManager.DeviceBasicInfo.deviceId

**系统能力：** SystemCapability.DistributedHardware.DeviceManager

**系统接口：** 此接口为系统接口。

